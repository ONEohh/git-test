# config.yaml
model:
  endpoint: "http://localhost:1234/v1/chat/completions"
  name: "qwen3.5-4b"
  temperature: 0.7
  max_tokens: 4096
  stream: true

memory:
  vector_db_path: "./memory"

search:
  engine: "duckduckgo"
  timeout: 5
  max_results: 5

agent:
  verbose: true
  max_iterations: 5